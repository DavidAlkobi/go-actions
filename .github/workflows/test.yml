name: Echo Test 


on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  echo:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo "${{vars.test}}"
    - name: Echo 2  
      run: echo "${{secrets.pass}}"
